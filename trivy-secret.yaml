rules:
  - id: permit_api_key_rule
    category: general
    title: Permit Api Key Rule
    severity: CRITICAL
    keywords:
      - permit_key
    regex: .*permit_key.*
    secret-group-name: secret
